---
import { Image } from "astro:assets";

import Base from "@/layouts/Base.astro";
import { getPosts, mediaUrl } from "@/lib/strapi";

const posts = await getPosts();
---

<Base title="Blog — Las Galias" description="Noticias y consejos sobre vivienda y finca raíz.">
  <section class="container py-12 md:py-16">
    <p class="text-label text-ink font-semibold uppercase">Blog</p>
    <h1 class="text-h1 text-ink mt-1 font-bold">Historias y consejos</h1>

    {
      posts.length > 0 ? (
        <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {posts.map((post) => (
            <article class="shadow-card group overflow-hidden rounded-xl bg-white" data-reveal>
              <a href={`/blog/${post.slug}`} class="block">
                {post.cover?.url && (
                  <div class="bg-surface aspect-[16/9] overflow-hidden">
                    <Image
                      src={mediaUrl(post.cover.url)}
                      alt={post.cover.alternativeText ?? post.title}
                      width={640}
                      height={360}
                      class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
                    />
                  </div>
                )}
                <div class="p-5">
                  <h2 class="text-h4 text-ink font-bold">{post.title}</h2>
                  {post.excerpt && (
                    <p class="text-body-sm text-ink-muted mt-2 line-clamp-3">{post.excerpt}</p>
                  )}
                </div>
              </a>
            </article>
          ))}
        </div>
      ) : (
        <p class="text-body text-ink-muted mt-8">Aún no hay entradas publicadas.</p>
      )
    }
  </section>
</Base>
