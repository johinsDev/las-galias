/**
 * Admin UI in Spanish. `locales` limits the language picker to Spanish, so a
 * new editor does not land on an English admin by default. Field labels come
 * from src/utils/admin-labels.ts, applied on boot.
 */
export default {
  config: {
    locales: ["es"],
  },
};
