{
  "kind": "collectionType",
  "collectionName": "faqs",
  "info": {
    "singularName": "faq",
    "pluralName": "faqs",
    "displayName": "Pregunta frecuente",
    "description": "Accordion questions. `audience` scopes them to a page — the foreign-buyer landing renders audience=exterior"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "question": {
      "type": "string",
      "required": true
    },
    "answer": {
      "type": "blocks",
      "required": true
    },
    "audience": {
      "type": "enumeration",
      "enum": ["general", "exterior"],
      "default": "general",
      "required": true
    },
    "topic": {
      "type": "enumeration",
      "enum": ["antes-de-comprar", "durante-la-compra", "posventa", "tramites"],
      "description": "Pestaña de «Resolvemos tus dudas» en la página de PQR. Vacío deja la pregunta fuera de esas pestañas"
    },
    "order": {
      "type": "integer",
      "default": 0
    }
  }
}
