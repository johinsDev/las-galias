// Intentionally empty. Tailwind v4 runs through @tailwindcss/vite (no PostCSS
// needed) and this stub stops Vite/postcss-load-config from walking up past
// the repo and picking up unrelated configs from the home directory.
export default { plugins: [] };
