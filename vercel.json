{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "bun install",
  "buildCommand": "turbo run build --filter=web && rm -rf .vercel/output && cp -R apps/web/.vercel/output .vercel/output"
}
