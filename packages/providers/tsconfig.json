{
  "extends": "@lasgalias/typescript-config/node-library.json",
  "compilerOptions": {
    // tsup inyecta baseUrl en el build de d.ts; TS6 lo reporta como deprecado.
    "ignoreDeprecations": "6.0",
    "types": ["node"]
  },
  "include": ["src", "tsup.config.ts"],
  "exclude": ["node_modules", "dist"]
}
